# VFlash-FM project page

Static project page for an anonymous ICLR 2027 submission.

Serve the folder as-is: `index.html` at the root, media under `assets/`.
All paths are relative, so it works at a domain root or under a subpath.

Local preview: `python3 -m http.server 8000` then open http://localhost:8000
